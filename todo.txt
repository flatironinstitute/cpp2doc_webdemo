TODO doc generation:

1. Add the following properties to YAML
    - `title`
    - `has-children` (if applicable)
    - `parent`
    - `grand_parent` (if applicable)

2. Remove excess underscores at end of file names / urls

3. Maybe add permalinks [Should we remove docs in the url? ex: cpp-api/nda/make-view]

Done:

1. change destination folder to docs/cpp-api